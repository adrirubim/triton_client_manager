# WebSocket server
fastapi==0.136.0
uvicorn==0.44.0
wsproto==1.3.2
orjson==3.11.6

# Configuration
PyYAML==6.0.3

# OpenStack / API
requests==2.33.1

# Docker
docker==7.1.0

# Triton Inference
# NOTE: Avoid pip resolver conflicts with extras + latest grpcio.
# We pin the base client and explicitly pin gRPC/HTTP deps below.
tritonclient==2.68.0
grpcio==1.80.0
aiohttp==3.13.5
geventhttpclient==2.3.9
numpy==2.4.4
protobuf==7.34.1

# MinIO / S3 / model tooling
boto3==1.42.92
safetensors==0.7.0
pydantic==2.13.3
Pillow==12.2.0

# Testing
pytest==9.0.3
pytest-cov==7.1.0
pytest-asyncio==1.3.0
httpx==0.28.1

# Lint / format (used by CI workflows)
ruff==0.15.11
black==26.3.1

# Observability / metrics
prometheus-client==0.25.0

# Security / JWT validation
PyJWT[crypto]==2.12.1
