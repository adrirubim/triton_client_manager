# WebSocket server
fastapi>=0.115.0
uvicorn>=0.30.0
wsproto>=1.2.0

# Configuration
PyYAML>=6.0.2

# OpenStack / API
requests>=2.32.0

# Docker
docker>=7.0.0

# Triton Inference
tritonclient[grpc,http]>=2.48.0
numpy>=1.26.0
protobuf>=5.26.0

# MinIO / S3 / model tooling
boto3>=1.35.0
safetensors>=0.4.0
# ONNX is only used for offline model analysis (never exposed to untrusted input
# in production traffic). We pin to the latest stable release and track known
# CVEs via pip-audit ignore rules in CI.
onnx==1.20.1
pydantic>=2.0.0
Pillow>=10.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Observability / metrics
prometheus-client>=0.20.0

# Security / JWT validation
PyJWT[crypto]>=2.8.0
