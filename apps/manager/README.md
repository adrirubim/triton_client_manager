# Triton Client Manager — Application

This directory contains the main Python application. **Full documentation is in the repository root.**

→ **[../README.md](../README.md)** — Overview, installation, testing, architecture, and all project docs.

### Quick commands

```bash
# From apps/manager/
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
python client_manager.py
```
