#
# Development and test dependencies for Triton Client Manager (apps/manager)
#
# Install together with runtime deps:
#   cd apps/manager
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt -r requirements-test.txt
#

# Runtime deps (pinned)
-r requirements.txt

# Testing (pinned)
pytest==8.4.2
pytest-asyncio==1.3.0
pytest-cov==5.0.0
coverage==7.6.0
websockets==12.0

# Linting / formatting (pinned)
black==24.4.0
ruff==0.5.0
isort==5.13.0
flake8==7.0.0
