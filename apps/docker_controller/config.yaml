gitlab_url: https://registry.example.com
project_id: 123

project: example_project
local_registry: localhost:5000
# token and token_name are injected from REGISTRY_TOKEN / REGISTRY_TOKEN_NAME env vars
