# Example MinIO/S3 configuration template.
# Do NOT commit real credentials here — use environment variables
# or pass credentials in the management payload (`payload.minio`).

access_key: YOUR_MINIO_ACCESS_KEY
secret_key: YOUR_MINIO_SECRET_KEY
region: YOUR_MINIO_REGION