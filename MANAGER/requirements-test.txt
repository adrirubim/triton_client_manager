coverage>=7.0.0
pytest>=8.0.0
pytest-cov>=5.0.0
ruff>=0.7.0
black>=24.0.0

#
# Development and test dependencies for Triton Client Manager (MANAGER)
#
# Install together with runtime deps:
#   cd MANAGER
#   python -m pip install --upgrade pip
#   pip install -r requirements.txt -r requirements-test.txt
#

# Testing
pytest>=9.0.0
pytest-asyncio>=0.23.0
websockets>=12.0
coverage>=7.6.0

# Linting / formatting
black>=24.4.0
ruff>=0.5.0
isort>=5.13.0

# Test dependencies (install: pip install -r requirements-test.txt)
-r requirements.txt

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# WebSocket client for smoke/integration tests
websockets>=12.0
